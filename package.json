{
  "name": "scrape-sdk-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "check:versions": "node scripts/check-versions.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
