---
title: Tavily Extract Provider
description: Optimized raw content extraction tailored for autonomous LLM research pipelines.
---

## Scope and Credentials

Import the adapter directly from `scrape-sdk/tavily`:

```ts title="scraper.ts"
import { createScrapeClient } from "scrape-sdk";
import { tavily } from "scrape-sdk/tavily";

export const scraper = createScrapeClient({
  provider: tavily(),
});

const result = await scraper.scrape("https://example.com");
console.log(result.markdown);
```
