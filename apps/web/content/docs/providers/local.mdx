---
title: Local Cheerio
description: Static HTML to markdown with no API key.
---

```ts
import { local } from "scrape-sdk/local";

local()
```

`fetch` + cheerio main-content selection (`article` / `main` / densest block) + turndown ATX markdown. No JavaScript rendering. Good as last fallback and for tests.
